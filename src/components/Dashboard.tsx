import './Dashboard.css'; // Import the external CSS file

const Dashboard = () => {
  return (
    <main className="container">
      <h1>Dashboard</h1>
      <div className='content'>This is the content section.</div>
    </main>
  );
}

export default Dashboard;
